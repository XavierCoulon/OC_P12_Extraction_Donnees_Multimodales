# Dépendances projet à installer dans l'image Airflow
# Ne pas inclure sqlalchemy ni psycopg2 : déjà fournis par Airflow (versions pinned)
feedparser>=6.0
datasets>=3.0
Pillow>=11.0
tqdm>=4.67
python-dotenv>=1.0
pyarrow>=23.0
